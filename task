#!/bin/bash
set -e
cd "$(dirname "${BASH_SOURCE[0]}")"

dev () {
  BIFF_ENV=dev clj -m biff.core
}

docs-dev () {
  cd slate
  bundle exec middleman server
}

docs () {
  (cd slate; bundle exec middleman build --clean)
  rsync -av --delete slate/build/ --exclude CNAME docs/
}

"$@"
